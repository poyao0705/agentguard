# AgentGuard

AgentGuard is a **policy engine for AI agents**.

It intercepts agent tool calls, evaluates governance rules, and decides whether actions should be **allowed, denied, or require approval**.

AgentGuard helps teams safely deploy autonomous AI agents by enforcing deterministic policy on tool execution.
